#include<bits/stdc++.h>
using namespace std;
struct node
{
    int arr[4];
};
// accoridngl for it
int main()
{
    int test,d,i,j,k,m,n;

     struct node a;
     struct node b;
     for(i=0;i<4;i++)
     {
         a.arr[i]=i;
         b.arr[i]=i;
     }
     // print it out with the countt accordingly for it
     // which is most equally likely and which is least accordingly for it
     //
     // cannot do the equality check for the structure accordingly for it
     // basically what to sya next about all these things accordingly for it
     // which is equally likely or can be differnt accoridnly for all
     if(a==b)
     {
         cout<<"unfortunately they are equal "<<endl;
     }

}
