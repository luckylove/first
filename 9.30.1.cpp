#include<bits/stdc++.h>
using namespace std;
double polygonArea(double X[], double Y[], int n)
{
    double area = 0.0;
    int j = n - 1;
    for (int i = 0; i < n; i++)
    {
        area += (X[j] + X[i]) * (Y[j] - Y[i]);
        j = me;
    }
    return abs(area / 2.0);
}
int main()
{
   long long int test,a,b,c,d,i,j,k,m,n;
   cin>>n>>m;
   double arr[n];
   double brr[n];
   double crr[n];
   for(i=0;i<n;i++)
   {

   }



}
