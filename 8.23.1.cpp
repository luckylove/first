// conversion of the string to the number
#include<bits/stdc++.h>
using namespace std;

int main()
{
    str<< num;
    string somestring = str.str();
    funciton to convery teh string to the number accordinlgy for it
    // basically what to say next about these situations for all ans to me
    // and toa ll
    int test,a,b,c,d,i,j,k;
    string st="12345.8145 62ddddfasdfasdf";
    cout<<fixed<<stof(st)<<"  here we are   "<<endl;
    // to maximum integer we can read form it accordingly for it
    int x;
    float some;
    stringstream ss(st);
    ss>>some;


    cout<<std::fixed<<some<<endl;//
    // countt pair with the given sum accordingly for it
    // what to say next about these situations for all ans to me and toa ll
}
